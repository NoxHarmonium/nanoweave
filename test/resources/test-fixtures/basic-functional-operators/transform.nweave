{
    "result": input.test_list
        map (num) -> (num + 2)
        filter (num) -> (num > 3)
        reduce (prev, next) -> (prev + next),
    "result2": ["a", "b", "c", "c", "d"]
        filter (str) -> (str == "c")
        reduce (prev, next) -> (prev ++ next),
    "result3": input.test_list
        map #(%1 + 2)
        filter #(%1 > 3)
        reduce #(%1 + %2),
    "result4": ["a", "b", "c", "c", "d"]
        filter #(%1 == "c")
        reduce #(%1 ++ %2)

}